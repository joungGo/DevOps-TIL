## 시스템 콜 래퍼 함수(System Call Wrapper Function)

### 시스템 콜의 한계

시스템 콜은 커널 기능을 사용하기 위한 **유일한 진입점**이지만,  
일반적인 C 함수 호출과는 달리 **CPU 실행 모드 전환**이 필요하다.

이 모드 전환은 CPU가 제공하는 **특수 명령어**를 통해서만 가능하며,  
이 명령어는 **어셈블리 언어 수준에서만 직접 사용**할 수 있다.

따라서 C 언어와 같은 고급 언어에서는 시스템 콜을 직접 호출할 수 없다.

---

## 시스템 콜 래퍼 함수의 필요성 ⭐

> [!note]
> 만약 사용자 프로그램이 시스템 콜을 직접 호출해야 한다면 다음과 같은 문제가 발생한다.
> - 아키텍처마다 다른 어셈블리 코드를 작성해야 함
> - 프로그램의 이식성이 크게 저하됨
> - 시스템 프로그래밍 난이도가 급격히 증가함
> 
> 이러한 문제를 해결하기 위해 운영체제는 **시스템 콜 래퍼 함수**를 제공한다.

### 1. 시스템 콜 래퍼 함수가 없는 경우 (직접 시스템 콜 호출)

```text
[ 사용자 모드 ]

프로그램 A                    프로그램 B
-----------------              -----------------
고급 언어로 작성된 프로그램    고급 언어로 작성된 프로그램
        |                              |
        v                              v
어셈블리 언어로 작성된           어셈블리 언어로 작성된
시스템 콜 호출 코드              시스템 콜 호출 코드
        |                              |
        +--------------+---------------+
                       |
                       v
                [ 커널 모드 ]
                     커널
```

### 의미

- 사용자 프로그램이 **직접 어셈블리 코드**를 작성해 시스템 콜을 호출
    
- 아키텍처 의존적
    
- 이식성 낮음
    
- 프로그램 작성이 복잡함
    

---

### 2. 시스템 콜 래퍼 함수를 사용하는 경우 (glibc)

```text
[ 사용자 모드 ]

프로그램 A                    프로그램 B
-----------------              -----------------
고급 언어로 작성된 프로그램    고급 언어로 작성된 프로그램
        |                              |
        v                              v
 ---------------------------------------------------  
 |                 시스템 콜 래퍼 함수               |
 |                     (glibc 제공)                 |
 ---------------------------------------------------  
        |                              |
        +--------------+---------------+
                       |
                       v
                [ 커널 모드 ]
                     커널
```

### 의미

- 사용자 프로그램은 **glibc가 제공하는 래퍼 함수만 호출**
    
- 래퍼 함수 내부에서 어셈블리 코드와 시스템 콜 수행
    
- 아키텍처 차이는 glibc가 흡수
    
- 사용자 프로그램은 이식성과 단순성을 유지
    

---

## 시스템 콜 래퍼 함수 동작 흐름 ⭐

> [!note] 사용자 프로그램 → 시스템 콜 전체 흐름
> 사용자 프로그램이 시스템 콜을 사용하는 과정은 다음과 같다.
> 
> 1. 사용자 프로그램이 C 함수 호출  
>    예: `pause()`
> 2. 해당 함수는 glibc에 구현된 시스템 콜 래퍼 함수
> 3. 래퍼 함수 내부에서 시스템 콜 번호와 인자를 설정
> 4. 어셈블리 코드에서 CPU 특수 명령어 실행
> 5. CPU 실행 모드가 사용자 모드 → 커널 모드로 전환
> 6. 커널이 시스템 콜 처리
> 7. 처리 결과 반환 후 사용자 모드로 복귀
>
> 이 과정에서 사용자 프로그램은 **어셈블리 코드나 커널 내부 구조를 전혀 알 필요가 없다**.

---

## 정적 라이브러리와 공유 라이브러리 관점

### 정적 링크

- 시스템 콜 래퍼 함수 코드가 실행 파일에 포함됨
    
- 실행 파일 크기가 증가
    
- 실행 시 외부 라이브러리에 의존하지 않음
    

### 공유(동적) 링크

- 실행 파일에는 래퍼 함수 호출 정보만 포함됨
    
- 프로그램 시작 시 glibc 공유 라이브러리가 메모리에 로드됨
    
- 여러 프로세스가 동일한 래퍼 함수 구현을 공유
    

---

### 정적 라이브러리 vs 공유 라이브러리 구조 (pause 예제)

#### 정적 라이브러리

```text
[ 실행 파일 ]

+----------------------------------+
| pause 프로그램                   |
|                                  |
|  - main()                        |
|  - pause()  (glibc 정적 코드)    |
+----------------------------------+
```

- 시스템 콜 래퍼 함수 코드가 실행 파일에 포함됨
    

---

#### 공유(동적) 라이브러리

```text
[ 실행 파일 ]              [ 메모리 ]
+------------------+        +----------------------+
| pause 프로그램   |        | libc.so (glibc)      |
|                  | -----> |  - pause()           |
|  - main()        | 호출   |  (시스템 콜 래퍼)    |
+------------------+        +----------------------+
```

- 프로그램 시작 시 공유 라이브러리가 로드됨
    
- 여러 프로세스가 하나의 래퍼 함수 구현을 공유
    

---

## 핵심 정리

- 시스템 콜은 고급 언어에서 직접 호출할 수 없다.
    
- 시스템 콜 실행에는 어셈블리 코드와 CPU 특수 명령어가 필요하다.
    
- glibc는 이를 감싸는 시스템 콜 래퍼 함수를 제공한다.
    
- 사용자 프로그램은 래퍼 함수만 호출하면 커널 기능을 사용할 수 있다.
    

---

## 한 문장 요약

시스템 콜 래퍼 함수는 고급 언어에서 직접 호출할 수 없는 시스템 콜을 감싸, 사용자 프로그램이 어셈블리 코드 없이도 커널 기능을 사용할 수 있도록 해주는 중간 계층이다.

---

다음으로 이어서 보면 이해가 더 단단해지는 주제는:

- 래퍼 함수와 실제 시스템 콜 번호 매핑
    
- glibc 래퍼 함수와 커널 API의 차이
    
- 직접 시스템 콜을 호출하는 예제와 비교