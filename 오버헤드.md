## 1️⃣ 오버헤드(Overhead)란?

**오버헤드**는  
👉 *“실제 일을 처리하는 데 직접적으로 기여하지는 않지만, 그 일을 하기 위해 추가로 필요한 비용”*이야.

여기서 **비용**은 보통:

- 시간 (CPU 사용)
    
- 메모리 사용
    
- 시스템 자원 사용  
    을 의미해.
    

### 예시

- 함수 호출 시 스택 프레임 생성
    
- 네트워크 통신 시 헤더 붙이기
    
- 프로세스 전환 시 컨텍스트 스위칭
    

이런 것들이 다 오버헤드야.

---

## 2️⃣ 동일한 프로그램을 여러 **프로세스**로 나눌 때 생기는 오버헤드

하나의 작업을 여러 프로세스로 나눠 처리하면, **병렬성**은 얻지만 대신 다음 오버헤드들이 생겨.

---

### ① 프로세스 생성 오버헤드

- 각 프로세스는 독립된:
    
    - 코드 영역
        
    - 데이터 영역
        
    - 힙
        
    - 스택
        
- 를 가짐
    
- OS가 PCB(Process Control Block)를 만들고 초기화해야 함
    

👉 **스레드 생성보다 무거움**

---

### ② 컨텍스트 스위칭 오버헤드

프로세스가 바뀔 때마다 OS는:

- 레지스터 상태 저장/복원
    
- 페이지 테이블 전환
    
- 캐시(TLB) 무효화 가능성
    

👉 **프로세스 전환은 스레드 전환보다 비용이 큼**

---

### ③ 프로세스 간 통신(IPC) 오버헤드

프로세스는 메모리를 공유하지 않으므로:

- 파이프
    
- 소켓
    
- 공유 메모리 + 동기화  
    같은 IPC가 필요함
    

👉 데이터 복사 + 커널 개입 발생

---

### ④ 메모리 중복 오버헤드

- 동일한 프로그램 코드라도
    
- 각 프로세스가 별도의 주소 공간을 가짐
    

👉 메모리 사용량 증가

---

## 3️⃣ 그런데 왜 “오버헤드가 많지 않다”고 말할 수 있을까?

여기가 진짜 중요한 포인트야.

---

### ① 실제 작업 시간에 비해 상대적으로 작다

대부분의 병렬 작업은:

- 연산량이 큼 (CPU-bound)
    
- 처리 시간이 김
    

👉 프로세스 생성/전환 비용은 **전체 작업 시간 대비 극히 일부**

예:

```
실제 계산: 10초
프로세스 오버헤드: 0.05초
```

→ 체감 거의 없음

---

### ② Copy-On-Write (COW) 덕분

현대 OS에서는:

- fork() 시 메모리를 바로 복사하지 않음
    
- 실제로 수정될 때만 복사
    

👉

- 코드 영역은 사실상 공유
    
- 메모리 중복 오버헤드 감소
    

---

### ③ 멀티코어 환경에서는 이득이 훨씬 큼

- 각 프로세스가 **다른 CPU 코어**에서 실행 가능
    
- 진짜 병렬 실행
    

👉 오버헤드보다 **병렬 처리로 얻는 성능 향상이 훨씬 큼**

---

### ④ OS와 하드웨어가 이걸 전제로 설계됨

- 컨텍스트 스위칭 최적화
    
- 캐시, TLB, 스케줄러 개선
    
- IPC 성능 향상
    

👉 “프로세스를 많이 쓰는 상황” 자체가 비정상이 아님

---

## 4️⃣ 한 문장 요약

> 여러 프로세스로 작업을 나누면 생성, 전환, 통신 등의 오버헤드가 발생하지만, 현대 OS에서는 이 비용이 실제 연산 시간과 병렬 처리로 얻는 성능 이득에 비해 매우 작기 때문에 전체 성능에 큰 영향을 주지 않는다.

---

원하면 다음으로:

- **왜 스레드는 더 가벼운지**
    
- **언제 프로세스를 쓰고 언제 스레드를 써야 하는지**
    
- **Java에서 멀티프로세스 vs 멀티스레드 선택 기준**
    
