# 멀티 스테이지 빌드란?

> 하나의 Dockerfile 안에서 여러 개의 `FROM`을 사용해  
> 빌드 단계와 실행 단계를 분리하는 방식

즉,

- 빌드에 필요한 도구는 빌드 단계에서만 사용
    
- 최종 이미지에는 실행에 필요한 파일만 포함
    

---

# 왜 필요한가?

예를 들어:

- Node.js 빌드에는 node, npm 필요
    
- Python 빌드에는 gcc, build-essential 필요
    
- Java는 maven/gradle 필요
    

하지만 실제 실행 시에는 이런 빌드 도구가 필요 없다.

멀티 스테이지를 사용하지 않으면:

- 이미지 용량 증가
    
- 보안 취약점 증가
    
- 불필요한 패키지 포함
    

---

# 기본 구조 예시 (Node.js)

```dockerfile
# 1단계: 빌드용
FROM node:20 AS builder
WORKDIR /app
COPY package.json package-lock.json .
RUN npm install
COPY . .
RUN npm run build

# 2단계: 실행용
FROM nginx
COPY --from=builder /app/dist /usr/share/nginx/html
```

---

# 동작 원리

1. `node:20`에서 빌드 수행
    
2. `/app/dist` 생성
    
3. 두 번째 `FROM nginx` 시작
    
4. builder 단계에서 필요한 결과물만 복사
    

최종 이미지에는:

- node 없음
    
- npm 없음
    
- 빌드 도구 없음
    
- dist 결과물만 존재
    

---

# Python 예시

```dockerfile
# 빌드 단계
FROM python:3.11-slim AS builder
WORKDIR /app
COPY requirements.txt .
RUN pip install --user -r requirements.txt

# 실행 단계
FROM python:3.11-slim
WORKDIR /app
COPY --from=builder /root/.local /root/.local
COPY ./app ./app
CMD ["python", "app/main.py"]
```

---

# 장점 정리

|장점|설명|
|---|---|
|이미지 용량 감소|빌드 도구 제거|
|보안 강화|불필요한 패키지 제거|
|빌드/실행 분리|구조 명확|
|캐시 활용 최적화|단계별 최적화 가능|

---

# 특정 단계만 빌드

```bash
docker build --target builder -t temp-image .
```

→ builder 단계까지만 빌드

디버깅할 때 유용하다.

---

# 한 줄 정리

멀티 스테이지 빌드는

> 빌드용 환경과 실행용 환경을 분리해  
> 가볍고 안전한 최종 이미지를 만드는 방법이다.

---

다음으로 깊게 갈 수 있는 주제:

- 실제 이미지 용량 비교
    
- 왜 CI/CD에서 멀티 스테이지가 필수인지
    
- Spring Boot에서의 멀티 스테이지
    
- distroless 이미지와 조합
    

어디까지 들어갈지 정해줘.