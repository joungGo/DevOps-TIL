## 디바이스 파일을 통한 장치 조작 흐름 (정확한 구조)

### 0. 전제

리눅스에서는 **장치도 파일처럼 취급**한다.  
`/dev/sda`, `/dev/tty`, `/dev/null` 같은 것이 디바이스 파일이다.

프로세스는 장치를 직접 제어하지 않는다.  
→ 반드시 **시스템 콜 → 커널 → 디바이스 드라이버**를 통해 접근한다.

---
# 전체 흐름 요약

```
[User Mode]
프로세스가 디바이스 파일을 사용해서 저장 장치에 접근하고 싶다고 디바이스 드라이버에 요청을 보냄
        ↓
cpu가 커널모드로 전환되고, 레지스터를 사용해서 장치에 요청을 전달 3. 장치가 요청을 처리
        ↓
드라이버 → 하드웨어 제어
        ↓
장치가 요청을 처리
        ↓
디바이스 드라이버가 요청 처리를 완료했다고 장치로 부터 결과를 전달 받음
        ↓
cpu가 사용자 모드로 전환되고 프로세스가 디바이스 드라이버로부터 결과를 전달 받음
```

---

## 1. 프로세스가 디바이스 파일에 접근 요청

예:

```c
int fd = open("/dev/sda", O_RDONLY);
read(fd, buf, size);
```

- 프로세스가 `open()`, `read()`, `write()` 같은 **시스템 콜 호출**
    
- 아직은 사용자 모드
    

---

## 2. CPU가 커널 모드로 전환

- 시스템 콜 인터럽트 발생
    
- CPU가 **사용자 모드 → 커널 모드 전환**
    
- 커널이 해당 파일이 **디바이스 파일인지 확인**
    
- 해당 파일에 연결된 **디바이스 드라이버의 함수 호출**
    

중요:  
CPU가 레지스터로 장치에 직접 요청하는 게 아니다.  
→ **커널 내부에서 드라이버 코드가 실행**된다.

---

## 3. 디바이스 드라이버가 장치에 요청 전달

드라이버가 하는 일:

- 장치 레지스터에 값 기록
    
- DMA 설정
    
- I/O 명령 전송
    

이때 실제 하드웨어가 동작한다.

---

## 4. 장치가 작업 수행

- 디스크라면 데이터 읽기/쓰기
    
- 네트워크 카드라면 패킷 송수신
    
- 완료되면 **인터럽트 발생**
    

---

## 5. 인터럽트 처리

- 장치 → CPU 인터럽트 신호 전송
    
- CPU가 다시 커널 모드에서 인터럽트 핸들러 실행
    
- 드라이버가 작업 완료 처리
    
- 결과를 커널 버퍼에 저장
    

---

## 6. 사용자 모드 복귀

- 시스템 콜 반환
    
- CPU가 사용자 모드로 복귀
    
- 프로세스가 결과를 전달받음
    

---

# 핵심 정리

1. 프로세스는 장치를 직접 접근하지 못한다.
    
2. 반드시 시스템 콜을 통해 커널로 진입한다.
    
3. 실제 장치 제어는 디바이스 드라이버가 한다.
    
4. 장치 완료는 인터럽트 기반으로 처리된다.
    
5. I/O는 대부분 블로킹이며, 완료 시까지 프로세스는 대기한다.
    

---

면접용 한 줄 정리:

"리눅스에서는 장치를 파일로 추상화하고, 프로세스는 시스템 콜을 통해 커널 모드로 진입하여 디바이스 드라이버를 통해 하드웨어를 제어합니다. 완료는 인터럽트 기반으로 처리됩니다."

더 깊게 들어가서 DMA, 블로킹/논블로킹 I/O, 버퍼 캐시까지 정리해줄까?