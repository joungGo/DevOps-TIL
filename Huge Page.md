## Huge Page

### 1. 기본 개념

Huge Page란 일반적인 페이지(보통 4KB)보다 **훨씬 큰 크기의 페이지**를 사용하는 메모리 관리 기법이다.  
리눅스에서는 대표적으로 다음과 같은 페이지 크기를 지원한다.

- 일반 페이지: 4KB
    
- Huge Page: 2MB
    
- (아키텍처에 따라) 1GB
    

Huge Page는 **연속된 물리 메모리**를 하나의 큰 페이지로 취급하여 매핑한다.

---

### 2. Huge Page가 필요한 이유

기본 페이지 크기(4KB)를 사용할 경우, 대용량 메모리를 사용하는 프로세스는 다음과 같은 비용을 부담한다.

- 페이지 수 증가 → 페이지 테이블 엔트리(PTE) 급증
    
- 페이지 테이블 계층 탐색(Page Walk) 비용 증가
    
- TLB 엔트리 소모 증가 → TLB 미스 빈도 증가
    

Huge Page는 이러한 문제를 완화하기 위해 도입되었다.

---

### 3. 페이지 테이블 관점에서의 Huge Page

Huge Page를 사용할 경우:

- 하나의 페이지 테이블 엔트리가
    
    - 여러 개의 4KB 페이지를 대신 표현
        
- 페이지 테이블의 깊이가 줄어들 수 있음
    

예시:

- 2MB Huge Page 1개 = 4KB 페이지 512개
    
- 즉, PTE 512개 → Huge Page 엔트리 1개
    

이로 인해:

- 페이지 테이블 크기 감소
    
- 주소 변환 시 탐색 단계 감소
    

---

### 4. TLB 관점에서의 Huge Page

TLB는 가상 주소 → 물리 주소 변환 결과를 캐싱한다.

- 일반 페이지 사용 시
    
    - 4KB 단위로 TLB 엔트리 소모
        
- Huge Page 사용 시
    
    - 하나의 TLB 엔트리가 훨씬 넓은 주소 범위를 커버
        

결과적으로:

- TLB 미스 감소
    
- 메모리 접근 성능 향상
    

이 효과는 대규모 메모리를 순차적으로 접근하는 워크로드에서 특히 크다.

---

### 5. 리눅스에서의 Huge Page 종류

리눅스에서는 크게 두 가지 방식의 Huge Page를 제공한다.

---

#### 5.1 Explicit Huge Page (HugeTLB)

- 커널 부팅 시 또는 런타임에 **미리 Huge Page를 예약**
    
- 해당 메모리는 일반 페이지로 사용 불가
    
- 애플리케이션이 명시적으로 Huge Page 사용 요청
    

특징:

- 물리 메모리 연속성 보장
    
- 성능 예측 가능
    
- 메모리 활용도는 떨어질 수 있음
    

주 사용처:

- 데이터베이스
    
- JVM
    
- 고성능 서버 애플리케이션
    

---

#### 5.2 Transparent Huge Page (THP)

- 커널이 자동으로 Huge Page 사용 여부를 판단
    
- 애플리케이션 수정 없이 적용 가능
    
- 가능한 경우에만 Huge Page로 승격(promotion)
    

특징:

- 사용 편의성 높음
    
- 항상 Huge Page로 매핑된다는 보장은 없음
    
- 메모리 단편화 상태에 영향을 받음
    

---

### 6. Huge Page와 Demand Paging의 관계

- Huge Page 역시 **Demand Paging** 원칙을 따른다.
    
- 실제 물리 메모리는
    
    - Huge Page 단위로
        
    - 접근 시점에 할당될 수 있다.
        

다만:

- Huge Page는 크기가 크기 때문에
    
- 페이지 폴트 발생 비용이 더 크고
    
- 물리 메모리 연속성 확보가 어려울 수 있다.
    

---

### 7. Huge Page의 장점과 단점

#### 장점

- 페이지 테이블 크기 감소
    
- TLB 효율 증가
    
- 메모리 접근 성능 향상
    

#### 단점

- 물리 메모리 단편화 문제
    
- 메모리 낭비 가능성 증가
    
- 작은 객체 위주의 워크로드에는 효과 제한적
    

> [!note] Huge Page 단점 개요  
> Huge Page는 메모리 접근 성능을 향상시키는 장점이 있지만,  
> **물리 메모리 단편화**, **메모리 낭비**, **워크로드 부적합성**이라는 구조적인 단점을 함께 가진다.

---

> [!warning] 단점 1. 물리 메모리 단편화 문제  
> Huge Page는 **큰 크기의 연속된 물리 메모리**를 필요로 한다.  
> 예를 들어 2MB Huge Page를 할당하려면, 물리 메모리 상에서도 연속된 2MB 공간이 존재해야 한다.
> 
> 시스템이 장시간 동작하면서 메모리 할당과 해제가 반복되면,  
> 물리 메모리는 작은 조각들로 나뉘는 **외부 단편화** 상태가 된다.  
> 이 경우 전체 여유 메모리는 충분하더라도,  
> Huge Page에 필요한 연속된 물리 공간을 확보하지 못할 수 있다.
> 
> 이 문제를 해결하기 위해 커널은 **메모리 컴팩션(compaction)** 을 수행할 수 있는데,  
> 이는 비용이 크고 시스템 지연(latency)을 유발할 수 있다.

---

> [!warning] 단점 2. 메모리 낭비 가능성 증가  
> Huge Page는 페이지 크기가 크기 때문에,  
> 실제 사용량보다 **큰 단위로 메모리를 점유**하게 된다.
> 
> 예를 들어 2MB Huge Page를 할당받았지만  
> 실제로 사용하는 데이터가 1.2MB라면,  
> 남은 0.8MB는 다른 프로세스가 사용할 수 없는 **내부 단편화 영역**이 된다.
> 
> 일반적인 4KB 페이지는 필요한 만큼만 세밀하게 할당할 수 있는 반면,  
> Huge Page는 이런 세밀한 메모리 관리가 어렵다.
> 
> 그 결과, 메모리 사용 패턴이 정교하지 않은 워크로드에서는  
> 전체 메모리 효율이 오히려 떨어질 수 있다.

---

> [!warning] 단점 3. 작은 객체 위주의 워크로드에는 효과 제한적  
> Huge Page의 주요 장점은  
> 페이지 테이블 엔트리 수 감소와 TLB 미스 감소이다.
> 
> 그러나 작은 객체를 중심으로 동작하는 워크로드에서는  
> 접근하는 메모리 범위 자체가 작고,  
> 이미 TLB에 잘 캐시되는 경우가 많다.
> 
> 이 경우 페이지 테이블 탐색이나 TLB 미스가  
> 성능 병목이 되지 않기 때문에,  
> Huge Page를 사용해도 성능 이점이 거의 없다.
> 
> 오히려 불필요하게 큰 페이지를 사용하면서  
> 메모리 낭비와 페이지 폴트 처리 비용만 증가할 수 있다.

---

> [!summary] 정리
> 
> - Huge Page는 **연속된 물리 메모리 요구**로 인해 단편화에 취약하다.
>     
> - 페이지 크기가 커서 **내부 단편화로 인한 메모리 낭비**가 발생할 수 있다.
>     
> - 작은 객체 위주의 워크로드에서는 **성능 개선 효과가 제한적**이다.
>     
> 
> 따라서 Huge Page는 모든 상황에 적용하는 최적화가 아니라,  
> 워크로드 특성을 고려해 선택적으로 사용해야 한다.

---

다음으로 원하면:

- Huge Page 전체 내용을 하나의 콜아웃 노트로 통합
    
- THP vs HugeTLB 비교 콜아웃
    
- 면접 답변용 콜아웃 요약  
    도 바로 만들어줄 수 있어.

---

### 8. 언제 Huge Page를 사용하는가

Huge Page는 다음 조건에서 특히 효과적이다.

- 대용량 메모리를 장시간 사용하는 프로세스
    
- 순차적 또는 예측 가능한 메모리 접근 패턴
    
- TLB 미스가 성능 병목인 경우
    

반대로:

- 메모리 사용량이 작거나
    
- 접근 패턴이 산발적인 경우  
    Huge Page의 이점은 크지 않다.
    

---

### 9. 한 문장 요약

Huge Page는 페이지 크기를 키워 페이지 테이블 엔트리 수와 TLB 미스를 줄임으로써, 대용량 메모리를 사용하는 애플리케이션의 메모리 접근 성능을 향상시키는 기법이다.

---

원하면 다음도 이어서 정리해줄 수 있다.

- Huge Page vs 일반 페이지 vs HugeTLB vs THP 비교
    
- Huge Page와 NUMA의 관계
    
- JVM에서 Huge Page를 사용하는 이유
    
- 면접에서 쓰기 좋은 정리 버전