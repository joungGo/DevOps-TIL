## 1. 처리 성능을 나타내는 주요 지표

### 1) 턴어라운드 타임 (Turnaround Time)

**턴어라운드 타임**은  
하나의 작업이 **시스템에 도착한 시점부터 완전히 종료될 때까지 걸린 전체 시간**이다.

포함 요소:

- 대기 시간
    
- 실행 시간
    
- I/O 대기 시간
    

즉,

> 사용자가 요청을 보내고 결과를 받을 때까지의 총 소요 시간

---

### 2) 처리 시간 (Processing Time / Service Time)

**처리 시간**은  
작업이 **실제로 CPU를 사용하여 처리된 시간**이다.

- CPU 사용 시간(user + sys)에 해당
    
- 대기 시간은 포함하지 않음
    

---

### 3) 평균 턴어라운드 타임

- 여러 작업의 턴어라운드 타임 평균
    
- 시스템 전체의 **응답성**을 판단하는 지표
    

사용자 체감 성능과 밀접하다.

---

### 4) 스루풋 (Throughput, 처리량)

**스루풋**은  
**단위 시간당 처리 완료된 작업의 개수**를 의미한다.

예:

- 초당 100 요청 처리
    
- 분당 1,000 트랜잭션 완료
    

스루풋은:

- 시스템의 **처리 능력**
    
- 배치 작업, 데이터 처리 시스템에서 중요
    

---

## 2. 논리 CPU 개수와 프로세스 개수의 관계

### 1) 논리 CPU ≥ 실행 가능한 프로세스 수

- 각 프로세스가 CPU를 독점 가능
    
- 컨텍스트 스위치 거의 없음
    
- 대기 시간 최소화
    
- 턴어라운드 타임 감소
    
- 응답 시간 매우 안정적
    

---

### 2) 논리 CPU < 실행 가능한 프로세스 수

- CPU 경쟁 발생
    
- 스케줄링 대기 시간 증가
    
- 컨텍스트 스위치 빈번
    
- 턴어라운드 타임 증가
    

이 경우:

- **스루풋은 일정 수준까지 증가 가능**
    
- 하지만 **개별 요청의 응답 시간은 급격히 악화**
    

---

## 3. CPU 사용률과 스루풋의 관계

CPU 사용률이 증가하면:

- 일정 구간까지는:
    
    - 스루풋 증가
        
- 임계점 이후에는:
    
    - 스루풋 증가 폭 감소
        
    - 응답 시간 급격히 증가
        

이유:

- 대기 큐 증가
    
- 캐시 미스 증가
    
- 컨텍스트 스위치 오버헤드 증가
    

[[CPU 사용률과 스루풋의 관계]]

---

## 4. 사용자 관점에서의 문제점 (웹 애플리케이션 예시)

### 상황 예시

- 웹 서버 논리 CPU: 4개
    
- 동시에 들어오는 요청: 20개
    
- 모든 요청이 CPU 연산 필요
    

### 결과

- 한 순간에 4개만 실행
    
- 나머지 16개는 대기
    
- 평균 응답 시간 증가
    
- 사용자는 “사이트가 느리다”라고 인식
    

중요한 점:

> 사용자는 스루풋이 높은지 낮은지를 느끼지 못한다.  
> **응답 시간만 느낀다.**

---

## 5. 왜 CPU 사용률을 낮게 유지해야 하는가

### 응답 성능이 중요한 시스템의 특징

- 웹 서비스
    
- API 서버
    
- 실시간 처리 시스템
    

이런 시스템에서는:

- 스루풋보다 **짧고 안정적인 응답 시간**이 중요
    

CPU 사용률이 낮으면:

- 요청이 들어왔을 때 즉시 처리 가능
    
- 큐 대기 시간 감소
    
- 예외 상황(트래픽 급증)에 대응 가능
    

---

## 6. 스루풋 중심 시스템과의 대비

### 스루풋이 중요한 시스템

- 로그 처리
    
- 배치 작업
    
- 데이터 분석
    

특징:

- CPU 사용률을 높게 유지해도 문제 없음
    
- 개별 작업 지연 허용 가능
    

---

### 응답 시간이 중요한 시스템

- 웹 애플리케이션
    
- 온라인 서비스
    

특징:

- CPU 사용률을 일부러 낮게 유지
    
- 여유 자원을 남겨 둠
    
- 안정적인 응답 제공
    

---

## 7. 핵심 개념을 한 문장으로 정리

> 응답 성능이 중요한 시스템에서는 스루풋 극대화보다 CPU 여유를 확보해 요청 대기 시간을 줄이는 것이 사용자 경험에 결정적인 영향을 준다.

---

## 직관적인 비유

- **스루풋**: 톨게이트를 하루에 몇 대 통과시켰는가
    
- **응답 시간**: 지금 톨게이트에서 얼마나 기다리느냐
    

사용자는 “오늘 몇 대 통과했는지”가 아니라  
“지금 내가 얼마나 기다리는지”에 민감하다.

---

이 내용 다음으로 이어서 정리하면 아주 좋다:

- 큐잉 이론(M/M/1)
    
- CPU utilization과 latency 곡선
    
- SLO / SLI / SLA
    
