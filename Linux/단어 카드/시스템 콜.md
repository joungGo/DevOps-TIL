## 프로세스가 커널에 처리를 요청하는 방법

프로세스가 커널에 특정 처리를 요청하는 방법은 **시스템 콜(System Call)** 이다.  
시스템 콜은 CPU가 제공하는 **특수한 명령어**를 실행함으로써 발생한다.

프로세스가 사용자 모드에서 시스템 콜을 호출하면, CPU 내부에서는 **예외(Exception) 이벤트**가 발생한다.  
이 예외를 계기로 CPU 실행 모드는 **사용자 모드에서 커널 모드로 전환**되며, 커널은 요청된 시스템 콜 번호와 인자를 확인한 뒤 해당 처리를 수행한다.

커널 처리가 완료되면, CPU는 자동으로 **사용자 모드로 복귀**하여 이후의 사용자 프로그램 명령어를 계속 실행한다.

---

## 시스템 콜 처리 과정에서의 검증

시스템 콜을 처리하기 전에 커널은 **프로세스로부터 전달된 요청이 올바른지 검증**한다.  
이 검증 과정에는 다음과 같은 내용이 포함된다.

1. **시스템 콜 번호 검증**
    
    - 요청된 시스템 콜 번호가 유효한지 확인
        
    - 정의되지 않은 시스템 콜 번호일 경우 오류 반환
        
2. **권한 검사 (Permission Check)**
    
    - 호출한 프로세스가 해당 작업을 수행할 권한이 있는지 확인
        
    - 예: 일반 사용자가 관리자 전용 자원에 접근하려는 경우 차단
        
3. **인자 유효성 검사 (Argument Validation)**
    
    - 사용자 공간 주소가 올바른지 확인
        
    - 접근 불가능한 메모리 주소나 커널 영역 주소가 전달되었는지 검사
        
4. **자원 접근 가능 여부 확인**
    
    - 파일 디스크립터가 유효한지
        
    - 대상 파일이나 장치가 현재 사용 가능한 상태인지 확인
        

이러한 검증 절차를 통해 커널은 **시스템 안정성과 보안**을 유지한다.

---

## 시스템 콜을 처리하는 시간 비율

논리 CPU가 어떤 유형의 작업을 수행하고 있는지는 `sar` 명령어를 통해 확인할 수 있다.  
`sar` 는 CPU 사용 시간을 여러 범주로 나누어 통계 정보를 제공한다.

### 주요 CPU 사용 비율 항목

- **%user**  
    사용자 모드에서 실행된 시간의 비율  
    일반 애플리케이션 코드가 CPU를 사용한 시간
    
- **%nice**  
    낮은 우선순위(nice 값이 적용된) 사용자 프로세스가 사용한 CPU 시간 비율
    
- **%system**  
    커널 모드에서 실행된 시간의 비율  
    시스템 콜 처리, 인터럽트 처리, 커널 내부 작업 등이 포함됨
    
- **%idle**  
    CPU가 아무 작업도 하지 않고 대기한 시간의 비율
    

### 해석 관점

- `%system` 비율이 높을수록  
    → 시스템 콜, I/O 처리, 커널 작업이 많다는 의미
    
- `%user` 비율이 높을수록  
    → 애플리케이션 연산 위주의 작업이 많다는 의미
    
- `%idle` 이 매우 낮은 경우  
    → CPU 자원이 포화 상태일 가능성이 있음
    

---

## 한 문장 요약

프로세스는 시스템 콜을 통해 사용자 모드에서 커널 모드로 전환되어 커널의 중재 하에 자원 접근을 수행하며, 커널은 요청의 유효성과 권한을 검증한 뒤 처리 결과를 사용자 모드로 반환한다.

---

다음으로 이어서 정리하면 좋은 주제는:

- 시스템 콜 vs 인터럽트 vs 트랩 차이
    
- 컨텍스트 스위칭과 시스템 콜의 관계
    
- I/O 바운드와 CPU 바운드 프로세스의 CPU 사용 비율 차이