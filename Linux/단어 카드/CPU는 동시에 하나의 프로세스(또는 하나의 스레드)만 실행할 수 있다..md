## 1️⃣ “CPU는 동시에 하나의 프로세스만 실행한다”의 정확한 의미

👉 **하나의 CPU 코어는, 아주 짧은 순간에도 딱 하나의 실행 흐름만 실제로 실행할 수 있다**는 뜻이야.

- 여기서 **동시에**란  
    → _같은 시점(same instant)_ 을 말해
    
- **프로세스**는 보통 _실행 단위_를 대표해서 쓴 표현이고  
    (정확히는 **스레드** 하나가 실행됨)
    

즉,

> **1코어 = 1순간 = 1스레드(=1실행 흐름)**

---

## 2️⃣ 그런데 우리는 왜 “여러 프로그램이 동시에 실행”되는 것처럼 느낄까?

### 👉 이유: 컨텍스트 스위칭 + 매우 빠른 전환

CPU는 이렇게 일해:

1. 프로세스 A 실행 (아주 짧게)
    
2. 컨텍스트 스위칭
    
3. 프로세스 B 실행
    
4. 컨텍스트 스위칭
    
5. 프로세스 C 실행
    
6. 반복…
    

이걸 **1초에 수천~수만 번** 반복함.

👉 인간의 눈에는 **동시에 실행되는 것처럼 보임**

이걸 **동시성(concurrency)** 이라고 해.

---

## 3️⃣ “프로세스”라고 말하지만 사실은 “스레드”다

엄밀하게 말하면:

- CPU가 직접 실행하는 건 **프로세스가 아니라 스레드**
    
- 프로세스는 **자원 묶음**
    
- 스레드는 **실행 흐름**
    

그래서 더 정확한 표현은:

> CPU는 동시에 하나의 **스레드**만 실행할 수 있다 (코어 기준)

---

## 4️⃣ 멀티코어 CPU에서는 어떻게 되나?

코어 수만큼 **진짜 동시에 실행 가능**해.

예:

- 4코어 CPU → 최대 4개의 스레드 **동시에 실행**
    
- 8코어 CPU → 최대 8개
    

이 경우는:

- 컨텍스트 스위칭이 아니라
    
- **물리적 병렬 실행(parallelism)**
    

---

## 5️⃣ 정리 표

|상황|실제 실행|
|---|---|
|1코어 CPU|한 순간에 1 스레드|
|여러 프로세스 실행|빠른 전환으로 번갈아 실행|
|멀티코어 CPU|코어 수만큼 동시에 실행|
|우리가 느끼는 동시 실행|착시 (동시성)|

---

## 6️⃣ 한 문장으로 정리

> CPU는 한 코어 기준으로 한 순간에 하나의 실행 흐름만 처리하며, 여러 프로그램이 동시에 실행되는 것처럼 보이는 이유는 매우 빠른 컨텍스트 스위칭 때문이다.

---

이 다음 단계로 보통 자연스럽게 이어지는 질문이:

- “그럼 **I/O 중에는 CPU가 뭐 해?**”
    
- “왜 스레드가 많으면 오히려 느려져?”
    
- “Java에서 스레드 수는 어떻게 정해야 해?”
    