# 리눅스 권한 (Permission)

리눅스에서 권한은 **파일·디렉토리·프로세스 등에 누가 무엇을 할 수 있는지 정의하는 규칙**이다.

커널은 접근 요청이 들어오면 **UID/GID와 권한 비트**를 기준으로 허용 여부를 판단한다.

---

# 1. 기본 권한 종류

권한은 3가지다.

|기호|의미|
|---|---|
|r|read (읽기)|
|w|write (쓰기)|
|x|execute (실행)|

---

# 2. 권한 대상 (3계층)

각 파일은 3개의 대상에 대해 권한을 가진다.

```text
owner / group / others
```

예:

```text
-rwxr-xr--
```

해석:

- owner → rwx
    
- group → r-x
    
- others → r--
    

---

# 3. 파일과 디렉토리에서의 의미 차이

## 파일

- r → 파일 내용 읽기
    
- w → 파일 수정
    
- x → 실행 가능
    

## 디렉토리

- r → 디렉토리 목록 조회 (ls)
    
- w → 파일 생성/삭제 가능
    
- x → 디렉토리 진입 가능 (cd)
    

디렉토리에서는 **x 권한이 특히 중요**하다.

---

# 4. 숫자 표현 방식 (Octal)

권한은 8진수로도 표현한다.

|권한|값|
|---|---|
|r|4|
|w|2|
|x|1|

예:

```bash
chmod 755 file
```

의 의미:

- owner → 7 (4+2+1 = rwx)
    
- group → 5 (4+1 = r-x)
    
- others → 5 (4+1 = r-x)
    

---

# 5. 권한 변경 명령어

```bash
chmod    # 권한 변경
chown    # 소유자 변경
chgrp    # 그룹 변경
```

---

# 6. 특수 권한

## 1) setuid (s)

- 실행 시 파일 소유자 권한으로 실행
    

## 2) setgid (s)

- 그룹 권한 상속
    

## 3) sticky bit (t)

- 디렉토리에서 파일 소유자만 삭제 가능
    
- 예: /tmp
    

---

# 7. 권한 판단 흐름

커널은 다음 순서로 검사한다.

1. 요청한 프로세스의 UID 확인
    
2. 파일 owner와 비교
    
3. 아니면 group 비교
    
4. 아니면 others 권한 적용
    

즉, 가장 먼저 일치하는 범주의 권한이 적용된다.

---

# 한 줄 정리

리눅스 권한은 owner, group, others에 대해 read·write·execute 권한을 정의하며, 커널은 UID와 GID를 기준으로 접근 가능 여부를 판단한다.