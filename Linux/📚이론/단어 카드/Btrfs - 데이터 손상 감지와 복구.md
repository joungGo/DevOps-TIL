## Btrfs의 데이터 손상 감지와 복구

Btrfs는 단순 저널링 파일 시스템이 아니라, **데이터 무결성(integrity)을 적극적으로 검증하는 파일 시스템**입니다. 핵심은 **체크섬 + Copy-on-Write + 트리 구조 관리**입니다.

---

# 1. 데이터 손상 감지 방식

## 1) 체크섬 기반 검증

Btrfs는 모든 데이터에 체크섬이 존재

- **모든 데이터 블록**
    
- **모든 메타데이터 블록**
    

에 대해 **체크섬(checksum)** 을 계산하여 별도의 메타데이터 영역에 저장합니다.

### 동작 과정

1. 파일을 디스크에 쓸 때  
    → 데이터 블록 생성  
    → 해당 블록의 체크섬 계산  
    → 체크섬을 메타데이터 트리에 저장
    
2. 파일을 읽을 때  
    → 디스크에서 데이터 블록 읽음  
    → 다시 체크섬 계산  
    → 저장된 체크섬과 비교
    
3. 값이 다르면  
    → **데이터 손상(corruption) 감지**
    

이 방식은 다음과 같은 문제를 감지할 수 있습니다.

- 디스크 비트 오류 (bit rot)
    
- 전원 문제로 인한 일부 블록 손상
    
- RAID 컨트롤러 오류
    
- 저장장치 내부 오류
    

ext4, XFS는 기본적으로 **메타데이터만 보호**하지만,  
Btrfs는 **데이터 자체까지 보호**합니다.

---

# 2. 손상 복구 방식

손상 감지 후의 복구는 **스토리지 구성에 따라 다릅니다.**

---

## 1) 단일 디스크일 경우

- 손상은 감지 가능
    
- 하지만 대체 데이터가 없으면 자동 복구 불가
    

즉,

- “손상됨”은 알 수 있음
    
- 그러나 원본을 복원할 수는 없음
    

---

## 2) RAID1 / RAID10 등 미러링 구성일 경우

여기서 Btrfs의 강점이 드러납니다.

예를 들어:

디스크 A와 디스크 B에 동일 데이터가 저장되어 있다고 가정

1. 읽기 시 체크섬 불일치 발생
    
2. 다른 디스크의 복사본을 읽음
    
3. 정상 데이터 확인
    
4. 손상된 블록을 정상 블록으로 자동 교체 (self-healing)
    

이 과정을 **self-healing**이라고 합니다.

즉,

- 감지
    
- 대체본 탐색
    
- 자동 복구
    

가 모두 이루어집니다.

---

# 3. Copy-on-Write와 정합성 유지

Btrfs는 CoW 파일 시스템입니다.

파일을 수정하면:

1. 기존 블록을 덮어쓰지 않음
    
2. 새로운 위치에 새 데이터 기록
    
3. 모든 쓰기가 완료된 뒤
    
4. 루트 트리 포인터를 한 번에 갱신
    

이 구조 덕분에:

- 쓰기 중 전원 장애 발생 시
    
- 기존 데이터는 그대로 유지됨
    

즉, 중간 상태의 불완전 데이터가 노출되지 않습니다.

---

# 4. Scrub 기능

Btrfs는 `scrub`이라는 기능을 제공합니다.

역할:

- 전체 파일 시스템을 주기적으로 검사
    
- 모든 블록의 체크섬 검증
    
- 손상 발견 시 복구 시도
    

RAID 환경에서는:

- 자동으로 정상 복사본에서 복구
    

이 기능은 장기간 사용 시 발생할 수 있는  
**조용한 데이터 손상(silent corruption)**을 잡아내는 데 매우 중요합니다.

---

# 5. 메타데이터 보호

Btrfs는 내부적으로 B-Tree 기반 구조를 사용합니다.

- 파일 정보
    
- 디렉터리 구조
    
- 체크섬 정보
    
- 할당 정보
    

모두 트리 형태로 관리하며,  
이 메타데이터 역시 체크섬 보호 대상입니다.

따라서:

- 파일 데이터 손상
    
- 파일 구조 손상
    
- 트리 구조 손상
    

까지 감지 가능합니다.

---

# 6. 정리

Btrfs의 데이터 손상 감지 및 복구 특징은 다음과 같습니다.

1. 모든 데이터에 체크섬 적용
    
2. 읽을 때마다 무결성 검증
    
3. RAID 환경에서 자동 self-healing
    
4. CoW 구조로 쓰기 중 장애에 안전
    
5. scrub로 전체 무결성 검사 가능
    

단,

- 단일 디스크에서는 “감지”만 가능하고
    
- 복구는 미러/백업이 있어야 가능
    

---

만약 원하시면  
ext4, XFS와 비교해서 “면접용 답변 구조”로 정리해드릴까요?