# 인터럽트 (Interrupt)

인터럽트는  
**장치가 작업 완료를 CPU에 직접 알리는 방식**이다.

CPU가 계속 확인하는 것이 아니라,  
**장치가 능동적으로 CPU의 흐름을 잠시 중단시키고 처리 루틴을 실행하게 한다.**

---

# 인터럽트 동작 흐름

1. 프로세스가 장치에 작업 요청
    
2. CPU는 다른 작업을 수행
    
3. 장치 작업 완료
    
4. 장치가 인터럽트 신호 발생
    
5. CPU가 현재 작업을 잠시 중단
    
6. 커널의 인터럽트 핸들러 실행
   (CPU가 호출하며, 미리 디바이스 드라이버가 인터럽트 컨트롤러 하드웨어에 등록해 둔 인터럽트 핸들러 처리를 호출하는 것임)
    
7. 처리 후 원래 작업으로 복귀
    

---
인터럽트 컨트롤러는 인터럽트 요청(IRQ)을 다루는데, 요청마다 서로 다른 인터럽트 핸들러를 등록할 수 있습니다.

---

# 폴링 vs 인터럽트 비교

## 1. 동작 방식 차이

|구분|폴링|인터럽트|
|---|---|---|
|상태 확인 주체|CPU|장치|
|동작 방식|CPU가 반복 확인|장치가 완료 알림|
|CPU 점유|계속 점유 가능|필요할 때만 사용|
|응답 방식|능동 확인|수동 대기 후 처리|

---

## 2. CPU 사용 측면

### 폴링

- CPU가 계속 레지스터를 읽음
    
- Busy Polling은 CPU 낭비 심함
    
- 주기적 Polling은 낭비 완화 가능
    

### 인터럽트

- CPU는 다른 작업 가능
    
- 완료 시에만 처리
    
- 일반적으로 CPU 효율 높음
    

---

## 3. 응답 지연 측면

### 폴링

- Busy Polling → 즉시 반응
    
- 주기적 Polling → 확인 주기에 따라 지연 발생
    

### 인터럽트

- 인터럽트 처리 오버헤드 존재
    
- 인터럽트 컨텍스트 전환 비용 발생
    

---

## 4. 오버헤드 비교

### 폴링 오버헤드

- 반복 검사 비용
    
- CPU 자원 지속 소비
    

### 인터럽트 오버헤드

- 인터럽트 진입/복귀 비용
    
- 문맥 저장/복원 비용
    

---

# 언제 어떤 것을 쓰는가

### 폴링이 적합한 경우

- 매우 짧은 작업
    
- 초저지연이 중요한 경우
    
- 고속 네트워크 처리
    
- 짧은 임계 구간
    

### 인터럽트가 적합한 경우

- 작업 완료 시간이 예측 불가
    
- I/O 대기 시간이 긴 경우
    
- CPU 자원 절약이 중요한 경우
    

---

# 핵심 차이 한 문장

> 폴링은 CPU가 장치 상태를 직접 확인하는 방식이고,  
> 인터럽트는 장치가 작업 완료를 CPU에 통지하는 방식이다.