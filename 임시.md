CRD는 **Custom Resource Definition**의 줄임말이야.

---

## 한 줄 정의

> **쿠버네티스에 “내가 만든 리소스 타입”을 추가하는 기능**

---

## 왜 필요하냐

쿠버네티스 기본 리소스:

- Pod
    
- Service
    
- Deployment
    

→ 이것만으로는 부족할 때가 있음

그래서:

> **나만의 리소스 (예: Redis, Kafka, App 등)를 정의해서 사용**

---

## 예시

```yaml
apiVersion: stable.example.com/v1
kind: MyApp
metadata:
  name: my-app
spec:
  size: 3
```

→ `MyApp`이라는 **새로운 리소스 타입**을 만든 것

---

## 어떻게 동작하냐

1. CRD 생성
    

```bash
kubectl apply -f crd.yaml
```

2. 그러면 새로운 리소스 생김
    

```bash
kubectl get myapp
```

---

## 핵심 구조

- **CRD**: “이런 리소스를 만들 수 있다” 정의
    
- **Custom Resource (CR)**: 실제 사용 객체
    

---

## 어디서 많이 쓰냐

- Operator 패턴
    
- Helm chart 내부 확장
    
- 복잡한 앱 관리 (DB, 메시지큐 등)
    

---

## 한 줄 정리

**CRD = 쿠버네티스에 새로운 리소스 타입을 추가하는 기능**