## 커널의 필요성

다수의 프로세스가 동시에 저장 장치와 같은 하드웨어 자원에 접근할 경우, 접근 순서가 제어되지 않으면 데이터 손상이나 무결성 오류가 발생할 수 있다.

이를 방지하기 위해 각 프로세스의 명령어 실행과 자원 접근을 제어하는 핵심 요소가 필요하며, 이 역할을 수행하는 것이 **커널(Kernel)** 이다.

커널은 하드웨어가 제공하는 **권한 분리 메커니즘**을 이용해 프로세스가 하드웨어 자원에 직접 접근하지 못하도록 제어한다.  
이 제어는 **CPU 실행 모드(mode)** 를 통해 이루어진다.

시스템에는 **사용자 모드(User Mode)** 와 **커널 모드(Kernel Mode)** 가 존재한다.  
일반 프로세스는 사용자 모드에서만 실행되며, 이 모드에서는 저장 장치나 메모리 관리 장치와 같은 핵심 하드웨어 자원에 직접 접근할 수 없다.

따라서 프로세스가 파일 입출력이나 장치 접근과 같은 작업을 수행하려면, **시스템 콜(System Call)** 을 통해 커널 모드로 전환되어 커널의 중재를 받아야 한다.

---

## 커널의 역할

- 프로세스와 스레드의 생성, 종료 및 **CPU 스케줄링을 통한 실행 순서 제어**
    
- 메모리, 저장 장치, 네트워크 장치 등 **시스템 자원의 관리 및 할당**
    
- 시스템 내부에서 실행되는 모든 프로세스가 공유하는 자원을 **중앙에서 일관성 있게 관리**하고, 각 프로세스에 안전하게 배분
    

---

## 추가 설명

### 1. “명령어 실행 순서 제어”의 정확한 의미

커널은 개별 프로세스 내부의 명령어 순서를 직접 바꾸지 않는다.  
대신 **어떤 프로세스를 언제 CPU에 올릴지 결정**함으로써, 결과적으로 프로세스 간 실행 순서를 제어한다.

이 기능은 **스케줄러(Scheduler)** 에 의해 수행된다.

### 2. 저장 장치 접근을 직접 막는 이유

저장 장치는 동시에 접근할 경우 데이터 일관성이 깨질 수 있는 대표적인 자원이다.  
커널이 모든 접근을 중재함으로써 다음을 보장한다.

- 동시에 여러 프로세스가 접근해도 데이터가 섞이지 않음
    
- 접근 권한이 없는 프로세스의 불법 접근 차단
    
- 파일 시스템의 무결성 유지
    

### 3. “커널을 거쳐야 한다”의 실제 동작

프로세스가 파일을 읽는 경우의 흐름은 다음과 같다.

1. 사용자 모드에서 `read()` 함수 호출
    
2. 시스템 콜 발생 → 커널 모드 전환
    
3. 커널이 권한 및 요청 유효성 검사
    
4. 커널이 장치 드라이버를 통해 저장 장치 접근
    
5. 결과를 사용자 모드로 반환
    

이 구조 덕분에 시스템 전체의 안정성과 보안이 유지된다.

---

원하면 다음 단계로:

- 사용자 모드 vs 커널 모드 비교 표
    
- 시스템 콜과 인터럽트 차이
    
- 커널 없이 동작한다면 발생할 문제들
    

중에서 이어서 정리해줄 수 있어.