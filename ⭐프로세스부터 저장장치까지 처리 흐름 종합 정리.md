## 1. 시작: 프로세스의 탄생과 메모리 구조

1-1. **프로세스 생성 (fork + COW)**

- 부모 프로세스가 `fork()`를 호출하면 자식 프로세스가 생성됩니다
- 이때 **Copy-On-Write(COW)** 기법이 작동합니다
    - 물리 메모리를 즉시 복사하지 않고, 페이지 테이블만 복사하여 같은 물리 페이지를 가리킵니다
      (부모/자식 간 같은 물리 메모리 공유)
    - 부모/자식 중 하나가 메모리를 수정하려 할 때 **페이지 폴트**가 발생하고, 그때서야 물리 페이지를 복사합니다
    - 이는 메모리와 시간을 크게 절약하는 최적화 기법입니다

1-2. **가상 메모리와 페이지 테이블**

```
프로세스 A의 가상 주소 공간:
0x00000000 ─────────┐
            [코드]   │
0x08000000  [데이터] │  ┌─────────────┐
            [힙]     ├─→│ 페이지 테이블 │─→ 물리 메모리
0xC0000000  [스택]   │  └─────────────┘
0xFFFFFFFF ─────────┘
```

- 각 프로세스는 독립된 **가상 주소 공간**을 가집니다
- **페이지 테이블**이 가상 주소를 물리 주소로 변환합니다 (MMU가 하드웨어적으로 수행)
- 페이지 테이블에는 각 페이지의 권한(읽기/쓰기/실행), 존재 비트, 더티 비트 등이 저장됩니다

## 2. 스케줄러: CPU 시간 배분

프로세스가 준비되면 **CPU 스케줄러**가 등장합니다:

1. **Ready Queue에 진입**: 실행 가능한 프로세스들이 대기합니다
2. **스케줄링 알고리즘** 작동:
    - CFS (Completely Fair Scheduler): Linux의 기본 스케줄러
    - 각 프로세스의 가상 런타임(vruntime)을 추적
    - 가장 적게 실행된 프로세스에 우선순위 부여
3. **컨텍스트 스위칭**:
    - 현재 프로세스의 레지스터 상태를 PCB(Process Control Block)에 저장
    - 다음 프로세스의 페이지 테이블로 전환 (CR3 레지스터 변경)
    - 다음 프로세스의 레지스터 상태 복원

## 3. 프로세스의 I/O 요청: 파일 쓰기

이제 프로세스가 파일에 데이터를 쓰려고 합니다:

```c
int fd = open("/dev/sda1", O_WRONLY);
write(fd, buffer, 4096);
```

### 1단계: 시스템 콜 → 커널 모드 전환

- `write()` 시스템 콜 발생
- **User Mode → Kernel Mode** 전환 (CPU 권한 레벨 변경)
- 커널의 시스템 콜 핸들러가 작동합니다

### 2단계: 디바이스 파일과 VFS

**디바이스 파일 (`/dev/sda1`)**

- 유닉스/리눅스는 “모든 것은 파일이다” 철학을 따릅니다
- 블록 디바이스는 `/dev` 디렉토리에 특수 파일로 존재합니다
- 각 디바이스 파일은 **major number**(드라이버 종류)와 **minor number**(디바이스 인스턴스)를 가집니다

```
$ ls -l /dev/sda1
brw-rw---- 1 root disk 8, 1  /dev/sda1
                        ↑  ↑
                     major minor
```

**VFS (Virtual File System)**

- 커널은 파일 시스템 종류(ext4, NTFS 등)에 관계없이 통일된 인터페이스 제공
- `write()` → VFS → 해당 파일 시스템의 드라이버 → 블록 디바이스 드라이버 순으로 호출됩니다

### 3단계: 커널 → 디바이스 드라이버

커널은 디바이스 드라이버의 함수를 호출합니다:

```c
// 간소화된 예시
struct block_device_operations {
    int (*open) (struct block_device *, fmode_t);
    int (*write) (struct block_device *, const char *buf, size_t count);
    ...
};
```

**디바이스 드라이버의 역할:**

1. 데이터를 DMA 가능한 커널 메모리 버퍼로 준비
2. 디바이스별 명령어 구성 (예: ATA 명령어)
3. 디바이스 하드웨어에 명령 전달

## 🔌 하드웨어 통신: MMIO와 폴링/인터럽트

### MMIO (Memory-Mapped I/O)

디바이스와 통신하는 방법:

```
물리 메모리 공간:
0x00000000 ─────────────
           [시스템 RAM]
0x7FFFFFFF ─────────────
0xFE000000 ─────────────  ← MMIO 영역
           [GPU 레지스터]
           [디스크 컨트롤러]
           [네트워크 카드]
0xFFFFFFFF ─────────────
```

- 디바이스의 제어 레지스터가 메모리 주소에 **매핑**됩니다
- 드라이버는 일반 메모리 읽기/쓰기로 디바이스를 제어합니다:

```c
volatile uint32_t *disk_command_reg = (uint32_t *)0xFE100000;
volatile uint32_t *disk_status_reg = (uint32_t *)0xFE100004;

*disk_command_reg = WRITE_COMMAND | sector_address;
```

### 폴링 vs 인터럽트

**폴링 방식:**

```c
// CPU가 계속 상태를 확인
while ((*disk_status_reg & BUSY_BIT) != 0) {
    // 디스크가 작업을 완료할 때까지 대기
}
```

- 장점: 구현 간단
- 단점: CPU 시간 낭비

**인터럽트 방식 (현대적 방법):**

1. 드라이버가 명령을 디바이스에 전달
2. 프로세스를 **I/O 대기 상태**로 전환 (Sleep)
3. 스케줄러가 다른 프로세스에 CPU 할당
4. 디스크 작업 완료 시 **하드웨어 인터럽트** 발생
5. CPU가 인터럽트 핸들러 실행
6. 대기 중이던 프로세스를 **Ready 상태**로 전환
7. 스케줄러가 다시 해당 프로세스에 CPU 할당

## 🔄 전체 흐름 요약

```
[사용자 프로세스]
    ↓ write(fd, buf, 4096)
    ↓ (가상 메모리: 페이지 테이블로 buf 주소 변환)
    ↓ 시스템 콜
    ↓
[커널 모드]
    ↓ VFS 레이어
    ↓ 파일 시스템 드라이버 (ext4)
    ↓ 블록 레이어 (I/O 스케줄러)
    ↓
[디바이스 드라이버]
    ↓ DMA 설정 (물리 주소 사용)
    ↓ MMIO로 디스크 컨트롤러에 명령 전달
    ↓
[프로세스 Sleep → 스케줄러가 다른 프로세스 실행]
    ↓
[저장장치]
    ↓ 데이터 쓰기 작업 수행
    ↓ 완료 시 인터럽트 발생
    ↓
[인터럽트 핸들러]
    ↓ 프로세스를 Ready 상태로 전환
    ↓
[스케줄러]
    ↓ 프로세스에 다시 CPU 할당
    ↓
[프로세스 재개]
    ↓ write() 시스템 콜 반환
    ↓ 사용자 모드로 복귀
```

## 🎯 핵심 연결 고리

1. **프로세스 & 가상 메모리**: 각 프로세스는 독립된 가상 주소 공간을 가지며, 페이지 테이블로 물리 메모리와 연결
2. **COW**: 프로세스 생성 시 메모리 효율성을 위해 페이지 테이블만 복사하고 실제 쓰기 시 복사
3. **스케줄러**: CPU 시간을 공정하게 배분하고, I/O 대기 중에는 다른 프로세스 실행
4. **시스템 콜**: User Mode ↔ Kernel Mode 전환의 게이트웨이
5. **디바이스 파일**: 하드웨어를 파일처럼 추상화하여 통일된 인터페이스 제공
6. **디바이스 드라이버**: 커널과 하드웨어 사이의 번역자 역할
7. **MMIO**: 메모리 주소로 하드웨어 레지스터 접근
8. **폴링/인터럽트**: 하드웨어 완료 통지 메커니즘으로 CPU 효율성 결정

이 모든 요소가 유기적으로 연결되어 사용자 프로그램이 하드웨어 복잡성을 의식하지 않고 간단한 `write()` 호출만으로 데이터를 저장할 수 있게 해줍니다! 🚀