- **`manage_master_user_password`** 이 설정을 하면 read replica 생성 지원이 되지 않는다는 오류가 발생합니다 그래서 host, password를 사용하는 방식으로 변경했는데, 이럴 경우 password에 대한 보안을 어떻게 챙기나요?
```bash
│ Error: creating RDS DB Instance (read replica) (urlshortener-postgres-replica): operation error RDS: CreateDBInstanceReadReplica, https response error StatusCode: 400, RequestID: 29c9444d-d41b-4666-869d-cc9be7ee507f, api error InvalidParameterValue: Creating read replicas for source instance with engine postgres where ManageMasterUserPassword is enabled is not supported.
│
│   with module.rds_replica.module.db_instance.aws_db_instance.this[0],
│   on .terraform/modules/rds_replica/modules/db_instance/main.tf line 30, in resource "aws_db_instance" "this":
│   30: resource "aws_db_instance" "this" {
```

- Primary / Read RDS 구분 시 트래픽이 자동으로 분산되는게 아니라 명시적으로 분산해서 보내야 하는데 방법이 여러개 있다.
	- SQLALchemy 다중엔진: 코드 수준에서 명시적 제어
	- RDS Proxy: AWS 관리형 연결 풀럴 연결 수 줄이기에 효과적
		- Pod 10개까지 스케일 시: 10 × 15 = 150 → t3.micro 한계 초과 → RDS Proxy 도입 시점
		- 이 경우 RDS 인스턴스 타입을 변경할 것인가? RDS Proxy를 도입할 것인가?

